<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <settings>
        <!-- 自动转换实体的属性和列名的规则（驼峰-下划线） -->
        <setting name="mapUnderscoreToCamelCase" value="true" />
        <!-- 默认枚举处理 -->
        <setting name="defaultEnumTypeHandler" value="org.apache.ibatis.type.EnumOrdinalTypeHandler"/>
        <!-- 自动映射，发现未知列（或者未知属性类型）抛出异常
        <setting name="autoMappingUnknownColumnBehavior" value="FAILING"/>
        -->
        <!-- 自动映射，任何复杂的结果集 -->
        <!-- <setting name="autoMappingBehavior" value="FULL"/>  -->
        
        <!--<setting name="cacheEnabled" value="true"/>-->
    </settings>
   <!-- <typeAliases>
        <package name="com.quliao.core.domain"/>
    </typeAliases>-->
   <!-- <typeHandlers>
        <typeHandler handler="org.apache.ibatis.type.EnumTypeHandler" javaType="com.quliao.core.domain.enums.SysCode" jdbcType="VARCHAR"/>
    </typeHandlers>-->
   <!-- <plugins>
        <plugin interceptor="com.github.pagehelper.PageInterceptor">
            &lt;!&ndash; 方言 &ndash;&gt;
            <property name="helperDialect" value="mysql"/>
            &lt;!&ndash; RowBounds对象的offset属性做为分页的page参数 &ndash;&gt;
            <property name="offsetAsPageNum" value="true"/>
            &lt;!&ndash; 分页合理化参数，防止因为页码头尾溢出而检索到空记录 &ndash;&gt;
            <property name="reasonable" value="true"/>
            &lt;!&ndash; 当 pageSize(limit) = 0 忽略页码，直接检索出所有的记录 &ndash;&gt;
            <property name="pageSizeZero" value="true"/>
            &lt;!&ndash; RowBounds 作为分页参数时需要检总记录数 &ndash;&gt;
            <property name="rowBoundsWithCount" value="true"/>
        </plugin>
    </plugins>-->
</configuration>